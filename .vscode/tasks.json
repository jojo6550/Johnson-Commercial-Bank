{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build bank",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o", "bank",
                "src/*.cpp",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compile all .cpp files in src/ and output to output/hospital_system"
        }
    ]
}